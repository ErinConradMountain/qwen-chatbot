# Example environment configuration for the Qwen chatbot
# Copy this file to `.env` and replace the placeholder values before running the app.

# Required: your OpenRouter API key (https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-


# Optional: override the default model announced to OpenRouter
MODEL_NAME=deepseek/deepseek-r1-0528-qwen3-8b:free

# Optional: require callers to include an auth header when using the web API
# CHATKIT_AUTH_TOKEN=choose-a-shared-secret
